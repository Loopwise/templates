// Tailwind CSS v4 is mostly zero-config.
// This file exists for any custom theme tokens you may want to add.
import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{ts,tsx}"],
};

export default config;
