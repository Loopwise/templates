{
  "name": "loopwise-templates",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "type-check": "turbo run type-check",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
